const userName:string[]=['admin','bugsbunny','daffyduck','sylvester','tweety'];
userName.forEach (userName=>{
    if(userName ==='admin')
        console.log('Hello admin, would you like to see a status report?');

{
    console.log(`Hellow ${userName}, than you for logging in again`);
}
})